version: v1
namespace: "script_exporter"
metrics:
  cpu:
    kind: gauge_vec
    interval: 3s
    matrix:
      column:
        - pcpu
        - vsz
        - rss
    command: |
      ps -eo {{ column }} | jq --slurp -R -cr '[split("\n")|.[]|tonumber?]|add'
